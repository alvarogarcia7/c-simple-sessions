/*
  WARNING: This is an autogenerated file.
  Any change to the output will be overridden on the next generation
  Prefer modifying the template file
*/

#include <string.h>
#include <stdbool.h>

#include "prod.h"

#define by_name(name, fn) else if (strcmp(name, function_name) == 0) { return &fn; }

void *find_function(char *function_name) {
    if(false) {} // so all the cases are in else if
    printf("Cannot find function name '%s'\n", function_name);
    return NULL;
}
